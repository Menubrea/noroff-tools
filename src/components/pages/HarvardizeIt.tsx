import { Box } from "@mui/material";
import { HarvardReference } from "..";

const HarvardizeIt = () => {
  return (
    <Box
      sx={{
        padding: { xs: 2, lg: 4 },
      }}
    >
      <HarvardReference />
    </Box>
  );
};

export default HarvardizeIt;
